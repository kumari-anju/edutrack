# 03 Students App

## Goal
Create the `students` app with full CRUD.

## Tasks
- Create Django app `students` inside `apps/`.
- Add `Student` model and migrations.
- Build CRUD views (class-based where possible).
- Add templates for add, edit, delete, and list.
- Register URLs for student routes.

When feature implemented successfully, move this file to `docs/completed/`.
