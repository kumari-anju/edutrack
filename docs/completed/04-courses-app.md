# 04 Courses App

## Goal
Create the `courses` app with full CRUD.

## Tasks
- Create Django app `courses` inside `apps/`.
- Add `Course` model and migrations.
- Build CRUD views (class-based where possible).
- Add templates for add, update, delete, and list.
- Register URLs for course routes.

When feature implemented successfully, move this file to `docs/completed/`.
