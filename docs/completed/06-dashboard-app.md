# 06 Dashboard App

## Goal
Create the `dashboard` app to show system statistics.

## Tasks
- Create Django app `dashboard` inside `apps/`.
- Add a dashboard view that shows:
  - total students
  - total courses
  - total enrollments
- Add a responsive Bootstrap dashboard template.
- Register the dashboard URL route.

When feature implemented successfully, move this file to `docs/completed/`.
