# 07 Models And Relations

## Goal
Define core data models and relationships.

## Tasks
- Create models:
  - `Student`
  - `Course`
  - `Enrollment`
  - `UserProfile`
- Use `ForeignKey` for enrollments and `OneToOneField` for user profiles.
- Register models in the admin.

When feature implemented successfully, move this file to `docs/completed/`.
