# 08 Templates And Static

## Goal
Set up reusable templates and static assets.

## Tasks
- Create `templates/base.html` and shared layout blocks.
- Add template folders for students, courses, and enrollments.
- Create `static/css`, `static/js`, and `static/images`.
- Use reusable templates and consistent UI.

When feature implemented successfully, move this file to `docs/completed/`.
