# 09 Documentation Structure

## Goal
Create the documentation folders and starter templates.

## Tasks
- Ensure `docs/` exists with:
  - `docs/to_start/`
  - `docs/completed/`
- Create starter markdown templates as needed.
- Use clear headings and sections in each doc.

When feature implemented successfully, move this file to `docs/completed/`.
